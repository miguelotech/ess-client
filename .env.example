# URL of your backend API
# For local development, this defaults to http://localhost:8080/api/dashboard in the code if not set.
# For production (Vercel), set this in the Vercel Project Settings.
NEXT_PUBLIC_API_URL=http://localhost:8080/api/dashboard
